import { createSlice } from '@reduxjs/toolkit';

export const initialState = {};

export const xxx = createSlice({
  name: 'xxx',
  initialState,
  reducers: {},
});

export const {} = xxx.actions;

export default xxx.reducer;
