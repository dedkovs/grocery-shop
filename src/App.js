import { Box } from '@mui/system';
import Button from '@mui/material/Button';

const App = () => {
  return (
    <Box sx={{ backgroundColor: 'lightGreen.main' }}>
      <Button variant="contained" color="primary">
        APP
      </Button>
    </Box>
  );
};

export default App;
